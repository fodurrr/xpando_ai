defmodule XPando.Repo.Migrations.AddNewsletterSubscriptions do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    create table(:newsletter_subscriptions, primary_key: false) do
      add(:id, :uuid, null: false, default: fragment("gen_random_uuid()"), primary_key: true)
      add(:email, :citext, null: false)
      add(:status, :text, null: false, default: "subscribed")
      add(:preferences, :map, default: %{})

      add(:subscribed_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")
      )

      add(:inserted_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")
      )

      add(:updated_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")
      )
    end

    create(
      unique_index(:newsletter_subscriptions, [:email],
        name: "newsletter_subscriptions_unique_email_index"
      )
    )
  end

  def down do
    drop_if_exists(
      unique_index(:newsletter_subscriptions, [:email],
        name: "newsletter_subscriptions_unique_email_index"
      )
    )

    drop(table(:newsletter_subscriptions))
  end
end
